import express = require('express');

class ValidateUser {

    static validateUser(): any {
        var app = express();
     
        return app;
    }
}

Object.seal(ValidateUser);
export = ValidateUser;